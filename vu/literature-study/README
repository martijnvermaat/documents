To generate PDF files from the sources, run

  make bibtex
  make

Required LaTeX packages:

  beamer (requiring pgf and xcolor)
  mathpartir
  amsmath
  amssymb
  amsthm
  sectsty
  hyperref
  hypcap
  svn-multi (svnkw in Debian)

For using svn-multi, make sure Subversion will interact with the package
by setting the svn:keywords property:

  svn propset svn:keywords 'Id' literature-study.tex
